{
  "name": "undim-video",
  "private": true,
  "type": "module",
  "scripts": {
    "watch": "tsc --watch",
    "build": "rm -rf dist && tsc && web-ext build --config=web-ext-config.mjs",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@tsconfig/strictest": "^2.0.8",
    "@types/firefox-webext-browser": "^143.0.0",
    "prettier": "^3.8.1",
    "typescript": "^6.0.2",
    "web-ext": "^10.1.0"
  },
  "prettier": {}
}
